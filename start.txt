uvicorn app.main:app --reload --port 8001


cd frontend
python -m http.server 5500


uvicorn app.main:app --reload --port 8004 - ai



docker qdrant:
docker run -d -p 6333:6333 qdrant/qdrant



curl -X DELETE http://localhost:6333/collections/kopilkin_memorypip install mem0ai[nlp]


Ollama пока оставляем локально на Mac, потому что она уже работает. В Docker-контейнере agent-service будет обращаться к Ollama через:
http://host.docker.internal:11434



docker exec -it kopilkin-postgres psql -U kopilkin -d savings_db
SELECT * FROM savings_goals;


docker compose up -d kafka kafka-ui


REDIS:

1. transaction-service
   key: summary:{user_id}
   purpose: cache calculated financial summary
   ttl: 60 seconds
   invalidation: on transaction.created / transaction.deleted

2. auth-service
   key: user:{user_id}
   purpose: cache user profile
   ttl: 300 seconds
   invalidation: on profile update

3. api-gateway later
   key: rate_limit:{client_ip}
   purpose: request rate limiting
   ttl: 60 seconds

------------
curl http://127.0.0.1:8002/transactions/e38fa670-175d-420e-b5c8-5e6de85cee17/summary
docker logs kopilkin-transaction-service --tail=50
x2
cache hit


curl http://127.0.0.1:8001/users/e38fa670-175d-420e-b5c8-5e6de85cee17
docker logs kopilkin-auth-service --tail=50
x2
------------

curl http://127.0.0.1:8000/health


docker compose up -d --build api-gateway

------------
Frontend :5500
   ↓
Nginx Load Balancer :8088
   ↓
api-gateway-1 :8000
api-gateway-2 :8000
   ↓
auth / transaction / savings / agent

-------------

frontend :5500
   ↓
nginx-balancer :8088
   ↓
api-gateway-1 / api-gateway-2
   ↓
microservices


grpc test:

curl http://localhost:8088/transactions/7cd18cec-c526-40a4-a4ab-30c20b8fcb21/summary


Frontend
↓ REST
API Gateway
↓ gRPC
Savings Service
↓ Kafka
Worker Service
↓
PostgreSQL + Transaction Service


frontend/app.js
↓
POST /grpc/goals/{goal_id}/operations
↓
api-gateway
↓
SavingsService.CreateGoalOperation через gRPC
↓
savings-service создаёт GoalOperation со статусом PENDING
↓
savings-service публикует goal.operation.created в Kafka
↓
worker-service обрабатывает операцию